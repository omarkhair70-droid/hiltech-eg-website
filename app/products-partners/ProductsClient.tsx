'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { productCategories, products, type ProductItem } from '@/content/products';
import { site } from '@/content/site';
import { getRFQWhatsappLink, normalizeRFQItem, readRFQItems, writeRFQItems, type RFQItem } from '@/lib/rfq';

function toRFQItem(item: ProductItem): RFQItem {
  return normalizeRFQItem({ id: item.id, name: item.name, category: item.category, brand: item.brand, specs: item.shortSpecs, quantity: 1 });
}

export default function ProductsClient() {
  const [items, setItems] = useState<RFQItem[]>([]);
  const [open, setOpen] = useState(false);
  const [justAdded, setJustAdded] = useState<string | null>(null);
  useEffect(() => setItems(readRFQItems()), []);
  useEffect(() => writeRFQItems(items), [items]);
  const count = useMemo(() => items.reduce((sum, item) => sum + item.quantity, 0), [items]);

  const updateItem = (id: string, patch: Partial<RFQItem>) => setItems((prev) => prev.map((entry) => (entry.id === id ? normalizeRFQItem({ ...entry, ...patch }) : entry)));

  const addToRFQ = (product: ProductItem) => {
    setItems((prev) => {
      const found = prev.find((entry) => entry.id === product.id);
      if (found) return prev.map((entry) => (entry.id === product.id ? { ...entry, quantity: entry.quantity + 1 } : entry));
      return [...prev, toRFQItem(product)];
    });
    setJustAdded(product.id);
    setTimeout(() => setJustAdded((curr) => (curr === product.id ? null : curr)), 1400);
  };

  return <><p className="mt-4 max-w-4xl text-slate-700">Infrastructure products, cabling systems, cabinets, connectivity components, and security-ready supplies organized for project-based procurement and availability requests.</p><p className="mt-3 rounded-lg border border-navy-900/10 bg-navy-900 px-4 py-2.5 text-sm text-white">Add items to your RFQ basket, define quantities, and send a structured request to HILTECH for availability and quotation.</p><section className="mt-6 rounded-xl border border-slate-200 bg-white p-3 md:sticky md:top-16 md:z-20 md:mt-8 md:bg-white/95 md:p-4 md:backdrop-blur"><h2 className="text-xl font-bold md:text-2xl">Browse by Category</h2><div className="mt-2 flex gap-2 overflow-x-auto pb-1 [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden md:flex-wrap md:overflow-visible"><a className="shrink-0 rounded-full border border-orange-200 bg-orange-50 px-3 py-1 text-xs font-semibold text-orange-700 shadow-sm md:px-4 md:py-1.5 md:text-sm" href="#all">All</a>{productCategories.map((category) => <a key={category} className="shrink-0 rounded-full border border-slate-300 bg-white px-3 py-1 text-xs font-medium text-slate-700 transition hover:border-slate-400 hover:bg-slate-50 md:px-4 md:py-1.5 md:text-sm" href={`#${category.toLowerCase().replace(/[^a-z0-9]+/g, '-')}`}>{category}</a>)}</div></section><section id="all" className="mt-8 space-y-10 pb-20 md:mt-10 md:space-y-12">{productCategories.map((category) => { const categoryProducts = products.filter((item) => item.category === category); const categoryId = category.toLowerCase().replace(/[^a-z0-9]+/g, '-'); return (<div key={category} id={categoryId} className="scroll-mt-28 md:scroll-mt-20"><h3 className="text-2xl font-bold tracking-tight text-slate-900">{category}</h3><p className="mt-1 text-sm text-slate-600">Category-aligned components selected to support compatibility, deployment quality, and procurement planning.</p><div className="mt-4 grid gap-4 md:mt-5 md:grid-cols-2 xl:grid-cols-3">{categoryProducts.map((item) => (<article key={item.id} className="flex h-full flex-col rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:border-orange-200 hover:shadow-md md:p-5"><div className="mb-4 h-1 w-12 rounded-full bg-orange-400" /><p className="mb-2 inline-flex w-fit rounded-full border border-orange-100 bg-orange-50 px-2 py-0.5 text-[9px] font-semibold uppercase tracking-wider text-orange-700 md:text-[10px]">{item.category}</p><h3 className="text-lg font-bold leading-tight text-slate-900">{item.name}</h3><div className="mt-3 space-y-1.5 text-sm text-slate-600"><p><span className="font-semibold text-slate-800">Brand:</span> {item.brand}</p><p><span className="font-semibold text-slate-800">Specs:</span> {item.shortSpecs}</p><p><span className="font-semibold text-slate-800">Use case:</span> {item.useCase}</p></div><p className="mt-4 rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-medium text-slate-600">Price and availability upon request</p><div className="mt-auto pt-3"><div className="flex flex-wrap gap-2"><button className="btn-primary px-4 py-2.5 text-sm" onClick={() => addToRFQ(item)} type="button">Add to RFQ</button><Link className="inline-flex items-center rounded-lg border border-slate-300 bg-white px-3.5 py-2 text-sm font-medium text-slate-600 transition hover:border-slate-400 hover:bg-slate-50" href="/contact">Request Availability</Link><a className="inline-flex items-center rounded-lg border border-slate-300 bg-white px-3.5 py-2 text-sm font-medium text-slate-600 transition hover:border-slate-400 hover:bg-slate-50" href={site.contact.whatsappRFQLink}>WhatsApp</a></div>{justAdded === item.id ? <p className="mt-2 text-xs font-medium text-emerald-700">Added to RFQ</p> : null}</div></article>))}</div></div>);})}</section><button type="button" onClick={() => setOpen(true)} className="fixed bottom-5 right-4 z-40 rounded-full bg-navy-900 px-4 py-3 text-sm font-semibold text-white shadow-lg ring-1 ring-black/10 transition hover:bg-navy-800 md:bottom-6 md:right-6">RFQ Basket ({count})</button>{open ? <div className="fixed inset-0 z-50"><div className="absolute inset-0 bg-slate-900/30" onClick={() => setOpen(false)} /><aside className="absolute right-0 top-0 h-full w-full max-w-xl overflow-y-auto bg-white shadow-2xl"><div className="sticky top-0 bg-navy-900 px-5 py-4 text-white"><div className="flex items-center justify-between"><h3 className="text-lg font-semibold">RFQ Basket ({items.length})</h3><button onClick={() => setOpen(false)} className="rounded-md border border-white/30 px-2 py-1 text-sm">Close</button></div><p className="mt-2 text-sm text-slate-100">Build your project supply request, add quantities and notes, then send it directly to HILTECH for availability and quotation.</p></div><div className="space-y-4 p-5">{items.length === 0 ? <div className="rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm"><p className="font-semibold text-slate-800">Your RFQ basket is currently empty.</p><p className="mt-1 text-slate-600">Add products to define your request faster.</p><Link href="/products-partners" onClick={() => setOpen(false)} className="mt-3 inline-flex rounded-lg bg-navy-900 px-3 py-2 text-white">Browse Products</Link></div> : items.map((item) => <article key={item.id} className="rounded-xl border border-slate-200 p-3"><h4 className="font-semibold text-slate-900">{item.name}</h4><p className="mt-1 text-xs text-slate-600">{item.category} • {item.brand}</p><div className="mt-2 flex items-center gap-2"><button className="rounded border px-2" onClick={() => updateItem(item.id, { quantity: item.quantity - 1 })}>-</button><span className="text-sm font-semibold">Qty: {item.quantity}</span><button className="rounded border px-2" onClick={() => updateItem(item.id, { quantity: item.quantity + 1 })}>+</button><input className="ml-auto w-20 rounded border border-slate-300 px-2 py-1 text-xs" value={item.unit} onChange={(e) => updateItem(item.id, { unit: e.target.value })} /></div><textarea className="mt-2 w-full rounded-md border border-slate-300 p-2 text-xs" rows={2} value={item.notes} onChange={(e) => updateItem(item.id, { notes: e.target.value })} placeholder="Add item notes" /><button className="mt-2 text-xs font-semibold text-red-600" onClick={() => setItems((prev) => prev.filter((entry) => entry.id !== item.id))}>Remove item</button></article>)}<div className="flex flex-wrap gap-2"><Link href="/rfq" onClick={() => setOpen(false)} className="btn-primary">Review RFQ</Link><a className="btn-primary bg-orange-500 hover:bg-orange-600" href={getRFQWhatsappLink(items)} target="_blank" rel="noreferrer">Send via WhatsApp</a><button className="inline-flex items-center rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700" onClick={() => setItems([])}>Clear Basket</button></div></div></aside></div> : null}</>;
}
